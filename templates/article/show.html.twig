{% extends 'base.html.twig' %}

{% block title %}Détails de l'Article{% endblock %}

{% block body %}
<div class="container mt-4">
    <div class="card shadow-sm">
        <div class="card-header bg-dark text-white">
            <h3 class="mb-0">Article: {{ article.titre }}</h3>
        </div>
        <div class="card-body">
            <table class="table">
                <tbody>
                    <tr>
                        <th>Id</th>
                        <td>{{ article.id }}</td>
                    </tr>
                    <tr>
                        <th>Titre</th>
                        <td>{{ article.titre }}</td>
                    </tr>
                    <tr>
                        <th>Contenu</th>
                        <td>{{ article.contenue }}</td>
                    </tr>
                    <tr>
                        <th>Image</th>
                        <td><img src="{{ article.image }}" alt="Image" class="img-fluid img-thumbnail"></td>
                    </tr>
                    <tr>
                        <th>Prix</th>
                        <td>{{ article.prixArticle }}€</td>
                    </tr>
                    <tr>
                        <th>Commentaire</th>
                        <td>{{ article.commantaire }}</td>
                    </tr>
                    <tr>
                        <th>J'aime</th>
                        <td>{{ article.nbJaime }}</td>
                    </tr>
                </tbody>
            </table>

            <a href="{{ path('app_article_index') }}" class="btn btn-secondary">Retour</a>
            <a href="{{ path('app_article_edit', {'id': article.id}) }}" class="btn btn-warning">Modifier</a>
            
            {{ include('article/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
