{% extends 'base.html.twig' %}

{% block title %}Liste des Articles{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        .articles-container {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 20px;
        }
        .article-card {
            width: 300px;
            border: 1px solid #ddd;
            border-radius: 10px;
            padding: 15px;
            box-shadow: 2px 2px 10px rgba(0, 0, 0, 0.1);
            background-color: white;
            text-align: center;
        }
        .article-card img {
            width: 100%;
            border-radius: 10px;
            height: 180px;
            object-fit: cover;
        }
        .article-card h3 {
            margin: 10px 0;
            font-size: 18px;
            font-weight: bold;
        }
        .article-info {
            margin-top: 10px;
            font-size: 14px;
            color: #555;
        }
        .likes-comments {
            display: flex;
            justify-content: space-between;
            margin-top: 10px;
        }
        .likes-comments span {
            font-size: 14px;
            color: #333;
        }
    </style>
{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1 class="mb-4 text-center">Articles</h1>

    <!-- Search Bar -->
    <div class="search-container text-right mb-3">
        <form method="get" action="{{ path('app_article_index') }}">
            <input type="text" name="search" placeholder="Rechercher un article..." value="{{ app.request.get('search') }}">
        </form>
    </div>

    <!-- Article Cards -->
    <div class="articles-container">
        {% for article in article %}
            <div class="article-card">
                <img src="{{ article.image }}" alt="Image">
                <h3>{{ article.titre }}</h3>
                <p class="article-info">{{ article.contenue }}</p>
                
                
                <!-- Likes and Comments Section -->
                <div class="likes-comments">
                    <span>👍 {{ article.nbJaime }} J'aime</span>
                    <span>💬 {{ article.commantaire }} Commentaires</span>
                </div>

                <div class="mt-3">
                    <a href="{{ path('app_article_show', {'id': article.id}) }}" class="btn btn-info btn-sm">Voir</a>
                    <a href="{{ path('app_article_edit', {'id': article.id}) }}" class="btn btn-warning btn-sm">Modifier</a>
                </div>
            </div>
        {% else %}
            <p class="text-center">Aucun article trouvé.</p>
        {% endfor %}
    </div>

    <div class="text-center mt-4">
        <a href="{{ path('app_article_new') }}" class="btn btn-primary">Créer un article</a>
    </div>
</div>


    <script>
        function scrollLeft() {
            document.getElementById("carousel").scrollBy({ left: -300, behavior: 'smooth' });
        }
        function scrollRight() {
            document.getElementById("carousel").scrollBy({ left: 300, behavior: 'smooth' });
        }
    </script>
{% endblock %}
