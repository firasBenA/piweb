{% extends 'base.html.twig' %}

{% block title %}DossierMedical{% endblock %}

{% block body %}
    <h1>DossierMedical</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dossierMedical.id }}</td>
            </tr>
            <tr>
                <th>DatePrescription</th>
                <td>{{ dossierMedical.datePrescription ? dossierMedical.datePrescription|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('dossier_medical2_index') }}">back to list</a>

    <a href="{{ path('dossier_medical2_edit', {'id': dossierMedical.id}) }}">edit</a>

    {{ include('dossier_medical2/_delete_form.html.twig') }}
{% endblock %}
