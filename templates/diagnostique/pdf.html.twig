<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Diagnosis Report</title>
    <style>
        body { font-family: Arial, sans-serif; }
        h1 { color: #2C3E50; text-align: center; }
        .container { width: 80%; margin: auto; padding: 20px; border: 1px solid #ddd; }
        p { font-size: 16px; line-height: 1.5; }
    </style>
</head>
<body>
    <div class="container">
        <h1>Diagnosis Report</h1>
        <p><strong>Patient:</strong> {{ diagnostique.patient.nom }}</p>
        <p><strong>Date:</strong> {{ diagnostique.date|date('Y-m-d H:i') }}</p>
        <p><strong>Symptoms:</strong> {{ diagnostique.symptomes|join(', ') }}</p>
        <p><strong>Diagnosis:</strong> {{ diagnostique.resultat }}</p>
        <p><strong>Prescription:</strong> {{ diagnostique.prescription }}</p>
    </div>
</body>
</html>
