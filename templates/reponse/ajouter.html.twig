{% extends 'base.html.twig' %}

{% block body %}
    <div class="container mt-5">
        <h2 class="text-center text-primary fw-bold mb-4">✍ Ajouter une Réponse</h2>

        {% for message in app.flashes('success') %}
            <div class="alert alert-success">
                {{ message }}
            </div>
        {% endfor %}

        <div class="card shadow-lg rounded">
            <div class="card-header bg-dark text-white">
                <strong>Formulaire de Réponse</strong>
            </div>
            <div class="card-body">
                {{ form_start(form, {'attr': {'novalidate': 'novalidate', 'data-turbo': 'false'}}) }}
                
                <div class="mb-3">
                    {{ form_label(form.contenu, null, {'label_attr': {'class': 'form-label'}}) }}
                    {{ form_widget(form.contenu, {
                        'attr': {
                            'class': 'form-control',
                            'required': 'required',
                            'rows': 5
                        }
                    }) }}
                    {{ form_errors(form.contenu, {'attr': {'class': 'text-danger fw-bold mt-1'}}) }}
                </div>

                <div class="d-flex justify-content-end">
                    <button type="submit" class="btn btn-primary px-4 py-2">Enregistrer</button>
                </div>

                {{ form_end(form) }}
            </div>
        </div>
    </div>

    <style>
        body {
            background-color: #f0f8ff;
        }

        .card {
            border-radius: 12px;
            overflow: hidden;
        }

        .card-header {
            background-color: #343a40;
        }

        .btn {
            transition: all 0.3s ease-in-out;
        }

        .btn:hover {
            transform: scale(1.05);
        }

        .form-control {
            border-radius: 8px;
        }
    </style>
{% endblock %}
