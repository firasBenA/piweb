{% extends 'base.html.twig' %}

{% block title %}Réservation de Rendez-vous{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="text-center text-primary fw-bold">📅 Réservation de Rendez-vous</h1>

        {# Affichage des messages flash #}
        {% for message in app.flashes('error') %}
            <div class="alert alert-danger" role="alert">
                {{ message }}
            </div>
        {% endfor %}
        
        {% for message in app.flashes('success') %}
            <div class="alert alert-success" role="alert">
                {{ message }}
            </div>
        {% endfor %}

        <div class="row justify-content-center">
            <div class="col-md-8 col-lg-6">
                <div class="card p-4 shadow-sm rounded" style="border: 1px solid #007bff;">
                    <h2 class="text-center mb-4">Détails du Rendez-vous</h2>
                    {{ form_start(form, {'attr': {'class': 'form', 'novalidate': 'novalidate'}}) }}

                    {# Champ Date du rendez-vous #}
                    <div class="mb-4">
                        {{ form_label(form.date, 'Date du rendez-vous', {'attr': {'class': 'form-label'}}) }}
                        {{ form_widget(form.date, {'attr': {'class': 'form-control', 'placeholder': 'Sélectionnez une date'}}) }}
                        <div class="text-danger mt-1">{{ form_errors(form.date) }}</div>
                    </div>

                    {# Champ Type de rendez-vous #}
                    <div class="mb-4">
                        {{ form_label(form.type_rdv, 'Type de rendez-vous', {'attr': {'class': 'form-label'}}) }}
                        {{ form_widget(form.type_rdv, {'attr': {'class': 'form-control', 'placeholder': 'Ex: Consultation, Urgence...'}}) }}
                        <div class="text-danger mt-1">{{ form_errors(form.type_rdv) }}</div>
                    </div>

                    {# Champ Cause du rendez-vous #}
                    <div class="mb-4">
                        {{ form_label(form.cause, 'Cause du rendez-vous', {'attr': {'class': 'form-label'}}) }}
                        {{ form_widget(form.cause, {'attr': {'class': 'form-control', 'placeholder': 'Décrivez la raison du rendez-vous'}}) }}
                        <div class="text-danger mt-1">{{ form_errors(form.cause) }}</div>
                    </div>

                    {# Champ Médecin #}
                    <div class="mb-4">
                        {{ form_label(form.medecin, 'Médecin', {'attr': {'class': 'form-label'}}) }}
                        {{ form_widget(form.medecin, {'attr': {'class': 'form-control'}}) }}
                        <div class="text-danger mt-1">{{ form_errors(form.medecin) }}</div>
                    </div>

                    <div class="text-center">
                        <button type="submit" class="btn btn-primary btn-lg shadow px-5 py-2">Réserver</button>
<a href="{{ path('patient_dashboard', {'id': patient.id}) }}" class="btn btn-secondary btn-lg shadow px-5 py-2 ml-2">
    Dashboard
</a>
                    </div>

                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>

    <style>
        body {
            background-color: #f8f9fa; /* Couleur de fond du body */
        }
        .card {
            transition: transform 0.3s;
        }
        .card:hover {
            transform: scale(1.02); /* Effet de zoom sur hover */
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.2); /* Ombre plus marquée */
        }
        .btn-primary {
            background-color: #007bff; /* Couleur personnalisée du bouton */
            border: none;
        }
        .btn-primary:hover {
            background-color: #0056b3; /* Couleur au survol */
        }
        .btn-secondary {
            background-color: #6c757d; /* Gris Bootstrap */
            border: none;
        }
        .btn-secondary:hover {
            background-color: #5a6268;
        }
        .form-label {
            font-weight: bold; /* Met en gras les labels */
        }
    </style>
{% endblock %}
